# Ethereum RPC endpoint (Infura, Alchemy, or your own node)
RPC_URL=https://mainnet.infura.io/v3/YOUR_KEY_HERE

# CoinGecko API key (optional — free tier works without one, but rate limits apply)
COINGECKO_API_KEY=

# Output directory for generated reports (default: ./reports)
OUTPUT_DIR=./reports
